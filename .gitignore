# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.eggs/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/

# ROS 2 build artifacts
/build/
/install/
/log/
src/*/build/
src/*/install/
src/*/log/

# Internal AI-planning specs — keep out of the public flagship repo
docs/superpowers/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Large generated benchmark embeddings (regenerate via extract_real_embeddings.py); fixture is committed
benchmark/data/real_embeddings_seed*.npz

# Real robomimic benchmark artifacts: regenerable rollout caches and the
# downloaded model-zoo checkpoint (large binary, not source). The eval scripts
# under benchmark/real/ are tracked; their cached inputs/outputs are not.
benchmark/real/cache/
benchmark/real/models/

.venv-bench/
